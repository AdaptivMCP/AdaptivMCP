# Test file for Joeys GitHub MCP server

This file was created via the `commit_file_async` tool on branch `ally/test-md`.

If you are reading this in GitHub, the end-to-end write path (authorize -> create_branch -> commit_file_async -> create_pull_request) is working.
