# NOTE:
# This file is used by the MCP server at runtime (per your setup), so it MUST include
# all runtime dependencies as well as developer tooling.

# -----------------------------
# Runtime (production) deps
# -----------------------------
httpx[http2]>=0.28.1
pydantic[email]>=2.11.7,<3.0.0

# Explicit transport pins for Uvicorn/H11 compatibility in hosted environments.
# Some hosts end up with mismatched uvicorn/h11 versions after partial upgrades.
# Keeping these explicit prevents "h11_max_incomplete_event_size" / loop_factory startup crashes.
h11==0.16.0
uvicorn==0.37.0

# FastAPI 0.120+ supports Starlette 0.49.x (required for CVE fixes below).
fastapi>=0.120.0,<0.121.0
fastmcp>=2.13.1,<3.0.0
mcp>=1.0.0

# Starlette pins:
# - 0.47.2+ mitigates CVE-2025-54121 (multipart rollover can block the event loop)
# - 0.49.1+ mitigates CVE-2025-62727
starlette>=0.49.1,<0.50.0

# -----------------------------
# Packaging / build / release
# -----------------------------
# Keep pip/setuptools explicit so fresh environments can build/install reliably.
pip>=25.3
setuptools>=80.9.0
wheel>=0.45.1
build>=1.4.0
pip-tools>=7.5.2
pipdeptree>=2.30.0
virtualenv>=20.36.1

twine>=6.2.0

# -----------------------------
# Testing
# -----------------------------
pytest>=9.0.2
pytest-asyncio>=1.3.0
pytest-cov>=7.0.0
pytest-timeout>=2.4.0
pytest-xdist>=3.8.0
coverage[toml]>=7.13.1
anyio>=4.12.0

# -----------------------------
# Lint / format / typecheck
# -----------------------------
ruff>=0.14.13
black>=25.12.0
isort>=7.0.0
flake8>=7.3.0
mypy>=1.19.1

# Type stubs
# (Add more stubs as mypy/pyright reports missing packages.)
types-python-dateutil>=2.9.0.20250822

# -----------------------------
# Security / supply-chain checks
# -----------------------------
bandit>=1.9.2
pip-audit>=2.10.0
safety>=3.7.0

# -----------------------------
# Developer experience
# -----------------------------
pre-commit>=4.5.1
python-dotenv>=1.1.0
ipython>=8.26.0

# Syntax highlighting for developer-facing log visuals
Pygments>=2.19.2
