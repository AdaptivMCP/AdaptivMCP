[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# Match Ruff defaults (and the repo's current style) to avoid large, noisy diffs.
select = ["E4", "E7", "E9", "F"]

[tool.black]
line-length = 100
target-version = ["py311"]
