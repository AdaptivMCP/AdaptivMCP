[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# Lint rules disabled (formatting can still be enforced via `ruff format`).
select = []

[tool.black]
line-length = 100
target-version = ["py311"]
