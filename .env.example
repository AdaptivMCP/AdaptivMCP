# Example environment configuration for GitHub MCP server
# Copy this file to .env and fill in the values for local Docker runs.

# REQUIRED: GitHub authentication
# Use a personal access token with at least `repo` scope (and `workflow` if you want Actions tools).
GITHUB_PAT=ghp_your_token_here
# Or alternatively (only one of these is required):
# GITHUB_TOKEN=ghp_your_token_here

# OPTIONAL: automatically enable write tools for this server
# 0 = write tools disabled until authorize_write_actions(approved=true) is called
# 1 = write tools enabled by default for all sessions
GITHUB_MCP_AUTO_APPROVE=1

# OPTIONAL: HTTP client tuning (defaults are usually fine)
# HTTPX_TIMEOUT=150
# HTTPX_MAX_CONNECTIONS=300
# HTTPX_MAX_KEEPALIVE=200
# HTTPX_HTTP2=1
# MAX_CONCURRENCY=80
# FETCH_FILES_CONCURRENCY=80
# TOOL_STDOUT_MAX_CHARS=12000
# TOOL_STDERR_MAX_CHARS=12000

# Docker / server port (internal container port is 8000 by default)
PORT=8000
