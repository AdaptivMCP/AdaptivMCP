# -----------------------------------------------------------------------------
# Adaptiv MCP GitHub Server (.env.example)
# -----------------------------------------------------------------------------
#
# This repo will load environment variables from a local ".env" file *if*
# python-dotenv is installed (see github_mcp/config.py).
#
# Usage:
#   cp .env.example .env
#   # then fill values in .env (never commit real secrets)
#
# Notes:
# - For GitHub auth, you typically need ONE token variable (GITHUB_TOKEN is
#   commonly used).
# - For Render API tools, you need ONE Render token variable.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# GitHub authentication (set at least ONE)
# -----------------------------------------------------------------------------
# Personal Access Token (classic or fine-grained, with appropriate scopes)
GITHUB_TOKEN=
# Alternative names the server also checks:
# GITHUB_PAT=
# GH_TOKEN=
# GITHUB_OAUTH_TOKEN=

# -----------------------------------------------------------------------------
# GitHub API
# -----------------------------------------------------------------------------
# Override for GitHub Enterprise / custom GitHub API base.
# Default: https://api.github.com
GITHUB_API_BASE=https://api.github.com

# -----------------------------------------------------------------------------
# Render API authentication (set ONE if using Render endpoints/tools)
# -----------------------------------------------------------------------------
RENDER_API_KEY=
# Alternatives:
# RENDER_API_TOKEN=
# RENDER_TOKEN=

# Optional Render API base override
# Default: https://api.render.com
RENDER_API_BASE=https://api.render.com

# -----------------------------------------------------------------------------
# Workspace / repo mirror configuration
# -----------------------------------------------------------------------------
# Base directory for persistent repo mirrors used by workspace tools.
# If unset, defaults under ~/.cache/mcp-github-workspaces (or XDG_CACHE_HOME).
# MCP_WORKSPACE_BASE_DIR=

# Timeout for applying diffs/patches to the repo mirror (0 disables).
# MCP_WORKSPACE_APPLY_DIFF_TIMEOUT_SECONDS=0

# -----------------------------------------------------------------------------
# HTTP client tuning
# -----------------------------------------------------------------------------
# Outbound HTTP timeout (seconds). Unset/empty or <=0 disables client timeout.
# HTTPX_TIMEOUT=
# Connection pool limits
# HTTPX_MAX_CONNECTIONS=300
# HTTPX_MAX_KEEPALIVE=200

# -----------------------------------------------------------------------------
# Concurrency and rate-limit behavior
# -----------------------------------------------------------------------------
# MAX_CONCURRENCY=80
# FETCH_FILES_CONCURRENCY=80

# Client-side GitHub rate limit retry behavior
# GITHUB_RATE_LIMIT_RETRY_MAX_ATTEMPTS=2
# GITHUB_RATE_LIMIT_RETRY_MAX_WAIT_SECONDS=30
# GITHUB_RATE_LIMIT_RETRY_BASE_DELAY_SECONDS=1

# Throttle GitHub search calls client-side (seconds). 0 disables.
# GITHUB_SEARCH_MIN_INTERVAL_SECONDS=0

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# LOG_LEVEL=INFO
# LOG_FORMAT=%(levelname)s | %(name)s | %(message)s

# Toggle ANSI coloring in logs
# ADAPTIV_MCP_LOG_COLOR=1

# Tool/request logging controls
# LOG_TOOL_CALLS=1
# LOG_TOOL_CALL_STARTS=1
# LOG_HTTP_REQUESTS=1
# LOG_HTTP_BODIES=1
# LOG_HTTP_MAX_BODY_BYTES=1000000

# Structured extras block controls
# LOG_APPEND_EXTRAS=1
# LOG_EXTRAS_MAX_LINES=200
# LOG_EXTRAS_MAX_CHARS=20000

# Reduce log noise by truncating/sanitizing payloads
# ADAPTIV_MCP_LOG_FULL_FIDELITY=1
# ADAPTIV_MCP_LOG_MAX_DEPTH=4
# ADAPTIV_MCP_LOG_MAX_LIST=50
# ADAPTIV_MCP_LOG_MAX_STR=500

# Inline request correlation context in log lines
# ADAPTIV_MCP_LOG_CONTEXT=1

# Shorten IDs/tokens in logs (useful on hosted providers)
# ADAPTIV_MCP_SHORTEN_TOKENS=0

# -----------------------------------------------------------------------------
# Git identity (used when the server makes commits in the workspace mirror)
# -----------------------------------------------------------------------------
# ADAPTIV_MCP_GIT_AUTHOR_NAME=Adaptiv MCP
# ADAPTIV_MCP_GIT_AUTHOR_EMAIL=adaptiv-mcp@local
# ADAPTIV_MCP_GIT_COMMITTER_NAME=Adaptiv MCP
# ADAPTIV_MCP_GIT_COMMITTER_EMAIL=adaptiv-mcp@local

# -----------------------------------------------------------------------------
# Optional internal/runtime knobs
# -----------------------------------------------------------------------------
# Include base64 content in certain tool payloads
# ADAPTIV_MCP_INCLUDE_BASE64_CONTENT=0

# Tool execution timeouts (0 disables)
# ADAPTIV_MCP_DEFAULT_TIMEOUT_SECONDS=0
# ADAPTIV_MCP_DEP_INSTALL_TIMEOUT_SECONDS=0
# ADAPTIV_MCP_PREFLIGHT_TIMEOUT_SECONDS=0
# ADAPTIV_MCP_TIMEOUT_COLLECT_SECONDS=0
# ADAPTIV_MCP_DISPATCH_PROBE_COOLDOWN_SECONDS=0
# ADAPTIV_MCP_WORKFLOW_DISPATCH_POLL_DEADLINE_SECONDS=0

# Optional sandbox content base (only relevant in certain deployments)
# SANDBOX_CONTENT_BASE_URL=
