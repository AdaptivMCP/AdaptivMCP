# Example environment configuration for the Adaptiv Controller GitHub MCP server
#
# Copy these variable names into your hosting provider (for example Render.com)
# and supply values appropriate for your deployment. Do NOT commit real secrets
# (tokens, passwords) to Git.

# ---------------------------------------------------------------------------
# GitHub authentication
# ---------------------------------------------------------------------------

# Required: GitHub personal access token used by the MCP server.
# Classic or fine-grained tokens are supported; scope them to the repositories
# you want this server to manage.
GITHUB_TOKEN=ghp_your_personal_access_token_here

# Optional: legacy name for the same token. If both are set, the code prefers
# GITHUB_PAT, otherwise it falls back to GITHUB_TOKEN. You usually only need
# to set one of these.
# GITHUB_PAT=ghp_your_personal_access_token_here

# ---------------------------------------------------------------------------
# Controller repo scoping
# ---------------------------------------------------------------------------

# The full name (owner/repo) of the controller repository. If you fork this
# project, set this to your fork (for example your-username/chatgpt-mcp-github).
# If unset, it defaults to Proofgate-Revocations/chatgpt-mcp-github.
# GITHUB_MCP_CONTROLLER_REPO=your-username/chatgpt-mcp-github

# The default branch that the server should treat as canonical for its own
# controller repo. For most users this will be "main".
# GITHUB_MCP_CONTROLLER_BRANCH=main

# ---------------------------------------------------------------------------
# Write gating and safety controls
# ---------------------------------------------------------------------------

# When set to a truthy value (1, true, yes, on), the server will start with
# WRITE_ALLOWED enabled, so tools tagged as write actions are immediately
# callable. When unset or set to a falsey value, write tools are blocked
# until the controller calls authorize_write_actions(approved=true). For most
# production deployments, leaving this unset (manual approval) is recommended.
# GITHUB_MCP_AUTO_APPROVE=0

# ---------------------------------------------------------------------------
# HTTP client tuning
# ---------------------------------------------------------------------------

# Base URL for the GitHub API. You can point this at a GitHub Enterprise
# endpoint if needed.
# GITHUB_API_BASE=https://api.github.com

# HTTP client timeouts and connection pooling. These defaults are chosen to
# work well for most small and medium deployments; you can tune them if you
# see timeouts or connection pressure in logs.
# HTTPX_TIMEOUT=150
# HTTPX_MAX_CONNECTIONS=300
# HTTPX_MAX_KEEPALIVE=200
# HTTPX_HTTP2=1

# ---------------------------------------------------------------------------
# Concurrency and file fetching
# ---------------------------------------------------------------------------

# Maximum number of concurrent GitHub API calls across tools.
# MAX_CONCURRENCY=80

# Maximum concurrency for fetch_files; defaults to MAX_CONCURRENCY when unset.
# FETCH_FILES_CONCURRENCY=80

# ---------------------------------------------------------------------------
# Git identity for commits
# ---------------------------------------------------------------------------

# Name and email used for Git commits created by this server. These identify
# the automation actor in Git history.
# GIT_AUTHOR_NAME=Adaptiv Controller
# GIT_AUTHOR_EMAIL=adaptiv@example.com
# GIT_COMMITTER_NAME=Adaptiv Controller
# GIT_COMMITTER_EMAIL=adaptiv@example.com

# ---------------------------------------------------------------------------
# Tool output truncation
# ---------------------------------------------------------------------------

# Upper bounds for stdout/stderr payloads returned from run_command and
# run_tests. Set to 0 or a negative value to disable truncation (not
# recommended for production, as very large outputs can be slow and hard to
# work with in ChatGPT).
# TOOL_STDOUT_MAX_CHARS=12000
# TOOL_STDERR_MAX_CHARS=6000

# ---------------------------------------------------------------------------
# Sandbox / external content helpers
# ---------------------------------------------------------------------------

# Optional base URL used when tools load sandbox:/ paths via HTTP instead of
# direct filesystem access. In typical ChatGPT-hosted setups this is managed
# for you; self-hosted deployments can leave this unset unless you know you
# need it.
# SANDBOX_CONTENT_BASE_URL=https://your-sandbox-proxy.example.com

# ---------------------------------------------------------------------------
# Logging and observability
# ---------------------------------------------------------------------------

# Log level for the github_mcp logger family (DEBUG, INFO, WARNING, ERROR).
# LOG_LEVEL=INFO

# Python logging format string. Most users can keep the default; advanced
# operators can override this to integrate with their logging stack.
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
